apiVersion: v1
kind: Service
metadata:
  name: mon-service
spec:
  type: NodePort
  selector:
    app: mon-appli
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001  # <--- Changez 3000 par 3001 ici
      nodePort: 30001